BEGIN;

CREATE TABLE IF NOT EXISTS cidade (
    id_cidade INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    nome VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    deleted BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE INDEX IF NOT EXISTS ix_cidade_nome_ativo ON cidade (lower(nome)) WHERE deleted = FALSE;

ALTER TABLE credor ADD COLUMN IF NOT EXISTS cnpj VARCHAR(18);
ALTER TABLE credor ADD COLUMN IF NOT EXISTS endereco VARCHAR(255);
ALTER TABLE credor ADD COLUMN IF NOT EXISTS bairro VARCHAR(120);
ALTER TABLE credor ADD COLUMN IF NOT EXISTS cep VARCHAR(9);
ALTER TABLE credor ADD COLUMN IF NOT EXISTS id_cidade INTEGER;
ALTER TABLE credor ADD COLUMN IF NOT EXISTS whats VARCHAR(30);
ALTER TABLE credor ADD COLUMN IF NOT EXISTS fone VARCHAR(30);
ALTER TABLE credor ADD COLUMN IF NOT EXISTS email VARCHAR(255);

DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'fk_credor_cidade') THEN
        ALTER TABLE credor ADD CONSTRAINT fk_credor_cidade FOREIGN KEY (id_cidade) REFERENCES cidade(id_cidade);
    END IF;
END $$;

CREATE INDEX IF NOT EXISTS ix_credor_id_cidade ON credor (id_cidade);

COMMIT;
